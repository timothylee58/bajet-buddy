import { SentinelDashboard } from "@/components/features/sentinel/SentinelDashboard";

export const metadata = { title: "Inflasi Watchdog | Bajet Buddy" };

export default function SentinelPage() {
  return <SentinelDashboard />;
}
